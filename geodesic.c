// geodesic.c

void set_geodesic(geodesic &g, int boundary, double length, double position, double angle)
{
        g.boundary = boundary;
        g.length = length;
        g.position = position;
        g.angle = angle;
}
